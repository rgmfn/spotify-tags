import * as React from 'react';
import { OverrideProps, DefaultComponentProps } from '@mui/types';
import { SelectTypeMap } from './SelectProps';
declare const Select: SelectComponent;
interface SelectComponent {
    <TValue extends {}, C extends React.ElementType>(props: {
        /**
         * The component used for the root node.
         * Either a string to use a HTML element or a component.
         */
        component: C;
    } & OverrideProps<SelectTypeMap<TValue>, C>): JSX.Element | null;
    <TValue extends {}>(props: DefaultComponentProps<SelectTypeMap<TValue>>): JSX.Element | null;
    propTypes?: any;
}
export default Select;
